let askMessage = "Guess the number !";

function askNumber() {
	let givenNumber = Number(prompt(askMessage));
}

askNumber();

// pour vérifier en affichant dans la console le nombre récupéré
// console.log(givenNumber); 
// pour vérifier en affichant dans la console le type du nombre récupéré
// console.log(typeof(givenNumber));